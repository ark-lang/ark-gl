#use std::mem
#use std::io

#use gl

pub type Game struct {};

pub func (Game) NewGame() -> ^Game {
	game := mem::alloc<Game>();
	@game = Game{};
	return game;
}

pub func (g: ^Game) render() {
	gl::setColor(255, 0, 255);
	gl::drawRect(0, 0, 40, 40);
}

pub func (g: ^Game) update() {
		
}

